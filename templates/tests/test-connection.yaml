apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tuleap.fullname" . }}-test-connection"
  labels:
    {{- include "tuleap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tuleap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
