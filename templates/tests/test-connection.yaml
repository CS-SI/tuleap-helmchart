apiVersion: v1
kind: Pod
metadata:
  name: "{{ template "tuleap.fullname" . }}-test-connection"
  labels:
    {{- include "tuleap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ template "tuleap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
